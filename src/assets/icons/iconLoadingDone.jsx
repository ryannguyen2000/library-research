import Lottie from "lottie-react";
import Icon from "@assets/svgs/icon_loading_done.json";

const IconLoadingDone = () => {
  return <Lottie animationData={Icon} loop={true} />;
};

export default IconLoadingDone;
